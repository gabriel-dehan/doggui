<div class="banner2" style="background-image: url('<%= asset_path'Chien.landing.jpg'%>');">
  <div class="banner-content">
    <h1> <i class="fas fa-bullhorn"></i> Votre avis compte beaucoup ! </h1>
  </div>
</div>


<div align="center">

  <div class="row">

    <%= simple_form_for @contact, :html => {:class => 'form-horizontal' } do |f| %>

      <div class="col-xs-12 col-sm-offset-4 col-sm-4">
        <div class="form-new-user">
            <%= f.input :name, label:"Votre pénom", :required => true %>
            <%= f.input :email, label:"Votre e-mail", :required => true %>
            <%= f.input :message, label:"Votre message", :input_html => {:rows => 10},:as => :text, :required => true %>
            <div class= "hidden">
              <%= f.input :nickname, :hint => 'Leave this field blank!' %>
            </div>
              </br>
              <%= f.button :submit, 'Envoyer mon message', :class=> "btn btn-warning" %>
        </div>
      <% end %>
        </div>

    </div>
</div>

<br>
<br>

